{
  "name": "chess-openings-analyzer",
  "version": "1.0.0",
  "description": "",
  "type": "module",
  "main": "dist/main.js",
  "scripts": {
    "build": "npm run typecheck && npm run bundle",
    "bundle": "esbuild src/main.ts --bundle --format=esm --sourcemap --outfile=dist/main.js",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^24.5.2",
    "esbuild": "^0.25.10",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4"
  },
  "dependencies": {
    "chess.js": "^1.4.0"
  }
}
